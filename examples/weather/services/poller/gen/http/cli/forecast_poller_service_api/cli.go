// Code generated by goa v3.25.3, DO NOT EDIT.
//
// Forecast Poller Service API HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/pulse/examples/weather/services/poller/design -o
// services/poller

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	pollerc "goa.design/pulse/examples/weather/services/poller/gen/http/poller/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"poller (add-location|subscribe|add-worker|remove-worker|status)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "poller add-location --city \"Santa Barbara\" --state \"CA\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	dialer goahttp.Dialer,
	pollerConfigurer *pollerc.ConnConfigurer,
) (goa.Endpoint, any, error) {
	var (
		pollerFlags = flag.NewFlagSet("poller", flag.ContinueOnError)

		pollerAddLocationFlags     = flag.NewFlagSet("add-location", flag.ExitOnError)
		pollerAddLocationCityFlag  = pollerAddLocationFlags.String("city", "REQUIRED", "")
		pollerAddLocationStateFlag = pollerAddLocationFlags.String("state", "REQUIRED", "")

		pollerSubscribeFlags     = flag.NewFlagSet("subscribe", flag.ExitOnError)
		pollerSubscribeCityFlag  = pollerSubscribeFlags.String("city", "REQUIRED", "")
		pollerSubscribeStateFlag = pollerSubscribeFlags.String("state", "REQUIRED", "")

		pollerAddWorkerFlags = flag.NewFlagSet("add-worker", flag.ExitOnError)

		pollerRemoveWorkerFlags = flag.NewFlagSet("remove-worker", flag.ExitOnError)

		pollerStatusFlags = flag.NewFlagSet("status", flag.ExitOnError)
	)
	pollerFlags.Usage = pollerUsage
	pollerAddLocationFlags.Usage = pollerAddLocationUsage
	pollerSubscribeFlags.Usage = pollerSubscribeUsage
	pollerAddWorkerFlags.Usage = pollerAddWorkerUsage
	pollerRemoveWorkerFlags.Usage = pollerRemoveWorkerUsage
	pollerStatusFlags.Usage = pollerStatusUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "poller":
			svcf = pollerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "poller":
			switch epn {
			case "add-location":
				epf = pollerAddLocationFlags

			case "subscribe":
				epf = pollerSubscribeFlags

			case "add-worker":
				epf = pollerAddWorkerFlags

			case "remove-worker":
				epf = pollerRemoveWorkerFlags

			case "status":
				epf = pollerStatusFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "poller":
			c := pollerc.NewClient(scheme, host, doer, enc, dec, restore, dialer, pollerConfigurer)
			switch epn {
			case "add-location":
				endpoint = c.AddLocation()
				data, err = pollerc.BuildAddLocationPayload(*pollerAddLocationCityFlag, *pollerAddLocationStateFlag)
			case "subscribe":
				endpoint = c.Subscribe()
				data, err = pollerc.BuildSubscribePayload(*pollerSubscribeCityFlag, *pollerSubscribeStateFlag)
			case "add-worker":
				endpoint = c.AddWorker()
			case "remove-worker":
				endpoint = c.RemoveWorker()
			case "status":
				endpoint = c.Status()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// pollerUsage displays the usage of the poller command and its subcommands.
func pollerUsage() {
	fmt.Fprintln(os.Stderr, `Service that polls weather forecasts and notifies subscribers.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] poller COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    add-location: Adds a location to the polling list.`)
	fmt.Fprintln(os.Stderr, `    subscribe: Subscribes to weather forecast notifications for a location.`)
	fmt.Fprintln(os.Stderr, `    add-worker: Adds a worker to the poller worker pool.`)
	fmt.Fprintln(os.Stderr, `    remove-worker: Removes a worker from the poller worker pool.`)
	fmt.Fprintln(os.Stderr, `    status: Provides poller status and statistics.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s poller COMMAND --help\n", os.Args[0])
}
func pollerAddLocationUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] poller add-location", os.Args[0])
	fmt.Fprint(os.Stderr, " -city STRING")
	fmt.Fprint(os.Stderr, " -state STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Adds a location to the polling list.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -city STRING: `)
	fmt.Fprintln(os.Stderr, `    -state STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "poller add-location --city \"Santa Barbara\" --state \"CA\"")
}

func pollerSubscribeUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] poller subscribe", os.Args[0])
	fmt.Fprint(os.Stderr, " -city STRING")
	fmt.Fprint(os.Stderr, " -state STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Subscribes to weather forecast notifications for a location.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -city STRING: `)
	fmt.Fprintln(os.Stderr, `    -state STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "poller subscribe --city \"Santa Barbara\" --state \"CA\"")
}

func pollerAddWorkerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] poller add-worker", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Adds a worker to the poller worker pool.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "poller add-worker")
}

func pollerRemoveWorkerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] poller remove-worker", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Removes a worker from the poller worker pool.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "poller remove-worker")
}

func pollerStatusUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] poller status", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Provides poller status and statistics.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "poller status")
}
